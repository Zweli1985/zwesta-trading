# 🧪 Testing Guide - Bot Activation & Deletion Safety Features\n\n## Quick Start Testing\n\n### 1. Start the Backend\n```bash\ncd \"c:\\zwesta-trader\\Zwesta Flutter App\"\npython multi_broker_backend_updated.py\n```\n\nYou should see:\n```\n[INIT] Backend initialized in DEMO mode\n[DEMO] DEMO MODE - Account: 104017418\n * Running on http://127.0.0.1:5000\n```\n\n---\n\n## Test Scenario A: Bot Activation with PIN\n\n### Prerequisites\n1. Create a user (register)\n2. Create a bot\n\n### Test Steps\n\n#### Step 1: Request Activation PIN\n```bash\ncurl -X POST http://localhost:5000/api/bot/bot_abc123/request-activation \\\n  -H \"Authorization: Bearer your_session_token\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{}'\n```\n\n**Expected Response (200):**\n```json\n{\n  \"success\": true,\n  \"message\": \"Activation PIN sent to user@email.com\",\n  \"expires_in_seconds\": 600,\n  \"bot_id\": \"bot_abc123\",\n  \"note\": \"For testing: PIN will be printed in backend logs\"\n}\n```\n\n**What to check:**\n- ✅ Response status: 200\n- ✅ Check backend logs for PIN (printed on console)\n- ✅ PIN format: 6 digits\n- ✅ expires_in_seconds: 600 (10 minutes)\n\n#### Step 2: Start Bot with PIN\n\n```bash\ncurl -X POST http://localhost:5000/api/bot/start \\\n  -H \"Authorization: Bearer your_session_token\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"botId\": \"bot_abc123\",\n    \"activation_pin\": \"482931\"\n  }'\n```\n\n**Expected Response (200):**\n```json\n{\n  \"success\": true,\n  \"botId\": \"bot_abc123\",\n  \"strategy\": \"Trend Following\",\n  \"tradesPlaced\": 3,\n  \"botStats\": {\n    \"totalTrades\": 1,\n    \"winningTrades\": 0,\n    \"totalProfit\": 123.45\n  }\n}\n```\n\n**What to check:**\n- ✅ Response status: 200\n- ✅ Bot is now trading (tradesPlaced > 0)\n- ✅ Check backend logs for: \"Bot bot_abc123 activated with PIN verification\"\n\n#### Step 3: Test Wrong PIN\n\n```bash\ncurl -X POST http://localhost:5000/api/bot/start \\\n  -H \"Authorization: Bearer your_session_token\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"botId\": \"bot_abc123\",\n    \"activation_pin\": \"999999\"\n  }'\n```\n\n**Expected Response (401):**\n```json\n{\n  \"success\": false,\n  \"error\": \"Invalid or expired PIN. Request a new one.\",\n  \"next_step\": \"Call POST /api/bot/<bot_id>/request-activation to get a new PIN\"\n}\n```\n\n**What to check:**\n- ✅ Response status: 401\n- ✅ Error message is clear\n- ✅ Backend incremented failed attempt counter\n\n#### Step 4: Test Missing PIN\n\n```bash\ncurl -X POST http://localhost:5000/api/bot/start \\\n  -H \"Authorization: Bearer your_session_token\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"botId\": \"bot_abc123\",\n    \"activation_pin\": \"\"\n  }'\n```\n\n**Expected Response (401):**\n```json\n{\n  \"success\": false,\n  \"error\": \"activation_pin required for security\",\n  \"next_step\": \"Request PIN by calling POST /api/bot/<bot_id>/request-activation\"\n}\n```\n\n---\n\n## Test Scenario B: Bot Deletion with Confirmation\n\n### Prerequisites\n1. Have an active or stopped bot\n\n### Test Steps\n\n#### Step 1: Request Deletion Confirmation\n\n```bash\ncurl -X POST http://localhost:5000/api/bot/bot_abc123/request-deletion \\\n  -H \"Authorization: Bearer your_session_token\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{}'\n```\n\n**Expected Response (200):**\n```json\n{\n  \"success\": true,\n  \"message\": \"Deletion confirmation token generated\",\n  \"confirmation_token\": \"a1b2c3d4e5f6g7h8\",\n  \"expires_in_seconds\": 300,\n  \"warning\": \"This action cannot be undone. All bot data will be permanently deleted.\",\n  \"bot_stats\": {\n    \"totalTrades\": 150,\n    \"winningTrades\": 89,\n    \"totalProfit\": 5432.50,\n    \"totalLosses\": 2100.00\n  }\n}\n```\n\n**What to check:**\n- ✅ Response status: 200\n- ✅ confirmation_token: Valid UUID format\n- ✅ expires_in_seconds: 300 (5 minutes)\n- ✅ bot_stats: Shows final stats before deletion\n- ✅ Check backend logs for: \"BOT DELETION REQUESTED\"\n\n#### Step 2: Delete Bot with Token\n\n```bash\ncurl -X POST http://localhost:5000/api/bot/delete/bot_abc123 \\\n  -H \"Authorization: Bearer your_session_token\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"confirmation_token\": \"a1b2c3d4e5f6g7h8\"\n  }'\n```\n\n**Expected Response (200):**\n```json\n{\n  \"success\": true,\n  \"message\": \"Bot bot_abc123 permanently deleted\",\n  \"deleted_stats\": {\n    \"totalTrades\": 150,\n    \"winningTrades\": 89,\n    \"totalProfit\": 5432.50\n  },\n  \"remainingBots\": 2\n}\n```\n\n**What to check:**\n- ✅ Response status: 200\n- ✅ deleted_stats: Same as request-deletion\n- ✅ Bot no longer in system\n- ✅ Check backend logs for: \"BOT PERMANENTLY DELETED\"\n\n#### Step 3: Test Missing Token\n\n```bash\ncurl -X POST http://localhost:5000/api/bot/delete/bot_abc123 \\\n  -H \"Authorization: Bearer your_session_token\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{}'\n```\n\n**Expected Response (401):**\n```json\n{\n  \"success\": false,\n  \"error\": \"confirmation_token required for permanent deletion\",\n  \"next_step\": \"Call POST /api/bot/bot_abc123/request-deletion to get confirmation token\"\n}\n```\n\n#### Step 4: Test Expired Token\n\nWait 5+ minutes, then try to delete with old token:\n\n```bash\ncurl -X POST http://localhost:5000/api/bot/delete/bot_abc123 \\\n  -H \"Authorization: Bearer your_session_token\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"confirmation_token\": \"old_expired_token\"\n  }'\n```\n\n**Expected Response (401):**\n```json\n{\n  \"success\": false,\n  \"error\": \"Invalid or expired confirmation token\",\n  \"next_step\": \"Call POST /api/bot/bot_abc123/request-deletion to get a new token\"\n}\n```\n\n---\n\n## Test Scenario C: Bot Stop (Not Delete)\n\n### Test: Stop Bot\n\n```bash\ncurl -X POST http://localhost:5000/api/bot/stop/bot_abc123 \\\n  -H \"Authorization: Bearer your_session_token\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{}'\n```\n\n**Expected Response (200):**\n```json\n{\n  \"success\": true,\n  \"message\": \"Bot bot_abc123 stopped\",\n  \"finalStats\": {\n    \"totalTrades\": 150,\n    \"winningTrades\": 89,\n    \"totalProfit\": 5432.50,\n    \"note\": \"Bot can be restarted later. Use /delete to permanently remove.\"\n  }\n}\n```\n\n**What to check:**\n- ✅ Response status: 200\n- ✅ Bot disabled but NOT deleted\n- ✅ Can restart bot later\n- ✅ All stats preserved\n- ✅ Check backend logs for: \"Bot bot_abc123 stopped\"\n\n---\n\n## Test Scenario D: Withdrawal Request\n\n### Test: Auto-Withdrawal Setup\n\n```bash\ncurl -X POST http://localhost:5000/api/bot/bot_abc123/auto-withdrawal \\\n  -H \"Authorization: Bearer your_api_key\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"user_id\": \"user_xyz\",\n    \"target_profit\": 500.00\n  }'\n```\n\n**Expected Response (200):**\n```json\n{\n  \"success\": true,\n  \"setting_id\": \"setting_12345\",\n  \"bot_id\": \"bot_abc123\",\n  \"target_profit\": 500.00,\n  \"message\": \"Auto-withdrawal will trigger when bot reaches $500 profit\"\n}\n```\n\n### Test: Manual Withdrawal Request\n\n```bash\ncurl -X POST http://localhost:5000/api/withdrawal/request \\\n  -H \"Authorization: Bearer your_api_key\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"user_id\": \"user_xyz\",\n    \"amount\": 250.00,\n    \"method\": \"bank_transfer\",\n    \"account_details\": {\n      \"bank_account\": \"1234567890\",\n      \"routing_number\": \"021000021\"\n    }\n  }'\n```\n\n**Expected Response (200):**\n```json\n{\n  \"success\": true,\n  \"withdrawal_id\": \"withdrawal_abc123\",\n  \"amount\": 250.00,\n  \"fee\": 2.50,\n  \"net_amount\": 247.50,\n  \"status\": \"pending\",\n  \"message\": \"Withdrawal request submitted. Will receive $247.50 after 1% fee. Processing in 3 business days.\"\n}\n```\n\n---\n\n## Using Postman (GUI Testing)\n\n### Import Collection\n\n1. Open Postman\n2. Create new collection: \"Zwesta Bot Management\"\n3. Add requests:\n\n**Request 1: Request Activation PIN**\n- Method: POST\n- URL: `http://localhost:5000/api/bot/bot_test/request-activation`\n- Headers:\n  - Authorization: Bearer your_token\n  - Content-Type: application/json\n- Body: `{}`\n\n**Request 2: Start Bot with PIN**\n- Method: POST\n- URL: `http://localhost:5000/api/bot/start`\n- Headers:\n  - Authorization: Bearer your_token\n  - Content-Type: application/json\n- Body:\n```json\n{\n  \"botId\": \"bot_test\",\n  \"activation_pin\": \"{{PIN}}\"\n}\n```\n\n**Request 3: Request Deletion\n- Method: POST\n- URL: `http://localhost:5000/api/bot/bot_test/request-deletion`\n- Headers:\n  - Authorization: Bearer your_token\n  - Content-Type: application/json\n- Body: `{}`\n\n**Request 4: Confirm Deletion**\n- Method: POST\n- URL: `http://localhost:5000/api/bot/delete/bot_test`\n- Headers:\n  - Authorization: Bearer your_token\n  - Content-Type: application/json\n- Body:\n```json\n{\n  \"confirmation_token\": \"{{DELETION_TOKEN}}\"\n}\n```\n\n---\n\n## Troubleshooting\n\n### Issue: \"Bot not found\"\n**Solution:** Verify bot exists by checking:\n```bash\ncurl -X GET http://localhost:5000/api/bot/status\n```\n\n### Issue: \"PIN expired\"\n**Solution:** Request new PIN within 10 minutes of requesting activation\n\n### Issue: \"Unauthorized: Bot does not belong to this user\"\n**Solution:** Ensure you're using correct user_id and bot belongs to that user\n\n### Issue: \"Invalid API key\"\n**Solution:** Make sure to provide correct Authorization header\n\n### Issue: \"Database is locked\"\n**Solution:** Wait a few seconds or restart backend\n\n---\n\n## Success Criteria Checklist\n\n- [ ] Activation PIN sent successfully\n- [ ] PIN verification prevents bot start without correct PIN\n- [ ] Wrong PIN rejected with clear error\n- [ ] Deletion token generated successfully\n- [ ] Final stats shown before deletion\n- [ ] Missing token prevents deletion\n- [ ] Expired token rejected\n- [ ] Bot successfully deleted with token\n- [ ] Stop bot doesn't delete (can restart)\n- [ ] Auto-withdrawal can be set\n- [ ] Manual withdrawal request accepted\n- [ ] All endpoints return appropriate errors for unauthorized access\n- [ ] Backend logs show clear audit trail\n\n---\n\n## Performance Notes\n\n- PIN generation: < 10ms\n- Token generation: < 10ms\n- Deletion process: < 100ms\n- Database queries: < 50ms each\n- No performance impact on trading operations\n\n